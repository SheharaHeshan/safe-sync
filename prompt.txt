this is what I want to build but it ts a different system you can see map, the menu bar you can understand all layout and elements.my project is a disaster information sharing system. which allows to agents we called they can log the system. after log the system then it show this pannel.full website have to be static.so this web template is place tracking system it is not what I want but I want that layout. this projet code have to using react web so build under React framework. the map integration is leaflet js opensource map.  so our user can plot mapping, also delete plots All plots are saved locally.